# CardValidator

- Please restore database using ScriptForCardValidator.sql or CardValidator.bak

- The unit test project is not finished.

- The card type is validated using columns : [ValidateRegex], [ExpiryYearCheckMethod],[ExpiryYearCheckType] in [CardTypes] table
